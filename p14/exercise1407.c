a)
long long_max(long x, long y)
{
	return x > y ? x : y;
}
b)
because it have space between usigned and long which are not allow 
for function name.
c)
we could use typedef unsigned long uLong
and send uLong to macro